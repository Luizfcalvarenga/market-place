<% if @single_chat && @single_chat.participant?(@single_chat, user) %>
  <% isActive = 'active' %>
<% else %>
  <% isActive = '' %>
<% end %>
<div class="user">
  <li id="list_item" class="list-group-item user_list_item <%= isActive %>">
    <div class="row">
      <div class="col-12 d-flex align-items-center justify-content-between">
        <div class="icon-container ms-3 mt-3">
          <%= link_to user, class: 'd-flex', data: {turbo: "false"} do %>
            <%= cl_image_tag(user.photo.key), class: 'chat_pfp' %>
            <% if user.full_name.present? %>
              <p class='ms-2 me-auto username'><%= user.full_name%></p>
            <% else %>
              <p class='ms-2 me-auto username'><%= user.fuemailll_name%> </p>
            <% end %>
          <% end %>
          <%= turbo_stream_from "user_status" %>
          <div>
            <%= render "users/status", user: user %>
          </div>
        </div>
        <% if user.full_name.present? %>
          <%= link_to user.full_name, user, class:'ms-2 me-auto username', data: {turbo: "false"}%>
        <% else %>
          <%= link_to user.email, user, class:'ms-2 me-auto username', data: {turbo: "false"}%>
        <% end %>
        <%= render 'users/notifications', chat: chat %>
      </div>
    </div>

    <%= turbo_frame_tag [dom_id(chat), "user_last_message"] do %>
      <%= render "users/last_message",
        last_message: last_message,
        sender: sender %>
    <% end %>
  </li>
</div>
